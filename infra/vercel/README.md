# Vercel

- Root directory: `apps/frontend`
- Framework preset: Next.js
- Build: `pnpm build` · Output: `.next`
- Install: `pnpm install --no-frozen-lockfile`

Variables: `NEXT_PUBLIC_API_BASE_URL`, `AUTH_SECRET`, `AUTH_URL`, `NEXT_PUBLIC_POSTHOG_KEY`.
